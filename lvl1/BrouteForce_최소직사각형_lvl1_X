# https://school.programmers.co.kr/learn/courses/30/lessons/86491
# 두 리스트 w,h 중 큰 수 리스트, 작은 수 리스트를 생성
# 각 리스트에서 최대값을 곱한 값이 정답

def solution(sizes):
    #w= []
    #h =[]
    #for i in sizes:
    #    w.append(max(i))
    #    h.append(min(i))
    #print(w)
    #print(h)
    
    return max(max(i) for i in sizes) * max(min(i) for i in sizes)